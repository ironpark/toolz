# planr 테스트 픽스처 목록
#
# 각 픽스처는 서로 다른 실행기가 사용하며, 격리된 작업 디렉터리로 복사된 뒤
# 그 안에서만 수정됩니다. 새 픽스처를 추가하면 이 파일에도 항목을 추가합니다.

fixtures:
  - name: plan-scenario
    used_by: planr/scripts/scenario.py
    workspace: planr/run/<timestamp>-scenario/
    description: >-
      단순 plan 생성 시나리오. 초안 본문 하나를 plan마다 다른 이름·의존성으로 다시
      써서 `apply`로 등록하고, 실제 planr phase 명령으로 완료/진행/대기/부분 완료 상태를 만든
      뒤 status·overview·notes 출력을 눈으로 확인한다. 실행기가 작업공간을 git 저장소로
      초기화한다. Go 프로젝트는 없으며 planr 바이너리만 필요하다.
    contents:
      - .planr.yaml: language, plans-active와 plans-archive 두 plans_dirs, bin/** ignore 설정
      - checkout-v2.md: 모든 plan 등록에 본문을 재사용하는 초안

  - name: codex-harness
    used_by: planr/scripts/codex.py
    workspace: 시스템 임시 디렉터리 (산출물은 planr/run/<timestamp>-codex/)
    description: >-
      Codex 평가용 샘플 저장소. 에이전트가 실제로 작업할 Go 프로젝트와
      새 JSON/stdin planr workflow를 설명하는 지침 문서를 담고 있으며, 하네스가 이 디렉터리 전체를 격리 작업공간으로
      복사한 뒤 git init을 수행한다.
    contents:
      - .planr.yaml: 훅으로 planr 이벤트를 .harness/planr-events.log에 기록
      - FIXTURE.PROMPT.<언어>.md: 실행 언어에 맞는 파일이 에이전트에게 보내는 요청. 워크스페이스로 복사하지 않는다
      - FIXTURE.AGENTS.<언어>.md: 실행 언어에 맞는 파일이 AGENTS.md로 설치되는 저장소 지침
      - go.mod, main.go, main_test.go: 에이전트가 수정하는 샘플 Go 프로젝트
      - README.md: 샘플 프로젝트 설명

  - name: codex-greenfield
    used_by: planr/scripts/codex.py --fixture codex-greenfield
    workspace: 시스템 임시 디렉터리 (산출물은 planr/run/<timestamp>-codex-greenfield/)
    description: >-
      백지 상태에서 시작하는 Codex 평가. Go 프로젝트가 없어 에이전트가 go.mod부터
      직접 만들어야 하며, 과제는 하위 명령·플래그·JSON 저장소·오류 처리를 갖춘
      할 일 관리 CLI다. codex-harness보다 요구사항이 많아 phase를 나눌 여지가 크다.
    contents:
      - .planr.yaml: 훅으로 planr 이벤트를 .harness/planr-events.log에 기록
      - .gitignore: .harness/, bin/, 빌드된 tasks 바이너리와 tasks.json 제외
      - FIXTURE.PROMPT.<언어>.md: 실행 언어에 맞는 파일이 에이전트에게 보내는 요청. 워크스페이스로 복사하지 않는다
      - FIXTURE.AGENTS.<언어>.md: 실행 언어에 맞는 파일이 AGENTS.md로 설치되는 저장소 지침
      - FIXTURE.TEST.sh: >-
          세션 종료 후 하네스가 워크스페이스 밖에서 실행하는 인수 검사.
          워크스페이스로 복사하지 않으므로 에이전트가 검사 내용을 보고 맞출 수 없다.
          `CHECK<TAB>이름<TAB>PASS|FAIL<TAB>비고` 줄을 출력하면 analyze.py가
          리포트의 "인수 검사" 표로 만든다.

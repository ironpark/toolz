---
plan_name: {{ .Name }}
description: {{ quote .Description }}
depends_on: [{{ join .DependsOn ", " }}]
---
<!--
planr draft. 모든 TODO(planr) 줄은 그 줄 자체를 내용으로 바꿔야 합니다. 표시 아래에
답을 덧붙이면 표시가 그대로 남고, `planr apply`는 표시가 남은 초안을 거부하며
남은 것을 한 번에 모두 알려 줍니다.

Phase yaml notes:
  phase       phase number, non-negative, unique within the plan
  slug        lowercase kebab-case, unique within the plan
  depends_on  other phases in THIS plan, by number or slug (e.g. [0] or [initial-work])
  status      planned or conditional
              conditional requires a non-empty entry_condition
              planned requires entry_condition: null
-->
# GOALS

## 문제와 사용자 관점의 최종 결과

## 측정 가능한 목표

## 지원 범위와 비목표

## reference source / commit / license

## 계획 전체의 완료 기준

# SCOPE

# CONTEXT

## 현재 구현과 병목

## 목표 구조와 invariant

# PHASES

## PHASE — Initial Work

```yaml
phase: 0
slug: initial-work
perf_phase: false
depends_on: []
status: planned
entry_condition: null
```

### 계획된 작업

- TODO(planr): 이 줄을 이 phase에서 수행할 작업으로 바꾼다.

### 완료 조건

- TODO(planr): 이 줄을 이 phase가 끝났다고 판정할 기준으로 바꾼다.

# VERIFICATION

# ORDERING

# NEXT

```yaml
next_phase: 0
```

TODO(planr): 이 줄을 다음에 진행할 phase에 대한 한 줄 설명으로 바꾼다.

node_modules/
.testcache
# 빌드된 웹 자산은 커밋한다.
#
# 저장소 루트의 .gitignore 가 dist/ 를 통째로 무시하지만, 이 디렉터리는
# go:embed 대상이라 없으면 node 없이 go build 조차 되지 않는다. CLI 하나만
# 받으면 되는 성질을 지키기 위해 예외로 둔다 (scripts/build-front.sh 가 만든다).
!internal/web/dist/
